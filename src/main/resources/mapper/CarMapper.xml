<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.learning_camp.mapper.CarMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.learning_camp.entity.Car">
        <id column="car_id" property="carId" />
        <result column="car_name" property="carName" />
        <result column="car_capacity" property="carCapacity" />
        <result column="driver_name" property="driverName" />
        <result column="driver_tel" property="driverTel" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        car_id, car_name, car_capacity, driver_name, driver_tel
    </sql>

</mapper>
